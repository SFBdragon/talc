# `talc` fuzzing

Run with `just fuzz manual` to fuzz the allocator and manual arena management functions.

Run with `just fuzz global_alloc` to fuzz the allocator with the `GlobalAllocSource` source.

Run with `just fuzz allocator` to fuzz the allocator with the `AllocatorSource` source.
